{
  "name": "cloudflare-photo-blog",
  "version": "1.0.0",
  "description": "Modern photo blog with Cloudflare Workers, D1, and R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "remote:migrate": "node scripts/migrate-remote.js",
    "local:migrate": "node scripts/migrate-all.js",
    "package": "node scripts/package.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "itty-router": "^4.0.20"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "wrangler": "^4.65.0"
  }
}
